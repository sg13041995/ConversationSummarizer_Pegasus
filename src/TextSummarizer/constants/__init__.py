from pathlib import Path

# path of config.yaml as a constant for reusability instead of hardcoding
CONFIG_FILE_PATH = Path("config/config.yaml")
# path of params.yaml as a constant for reusability instead of hardcoding
PARAMS_FILE_PATH = Path("params.yaml")